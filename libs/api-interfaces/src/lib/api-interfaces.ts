export interface Message {
  message: string;
}

export interface LoginCredential {
  email: string;
  role: string;
  username: string;
  phone: string;
  is_allowed: string;
  access_token: string;
}
